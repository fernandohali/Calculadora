/* function insert(num) {
  var numero = document.getElementById("soma").innerHTML;
  document.getElementById("soma").innerHTML = numero + num;
}
function clean() {
  document.getElementById("soma").innerHTML = "";
}
function back() {
  var soma = document.getElementById("soma").innerHTML;
  document.getElementById("soma").innerHTML = soma.substring(
    0,
    soma.length - 1
  );
}
function calcular() {
  var resultado = document.getElementById("soma").innerHTML;
  if (resultado) {
    document.getElementById("resultado").innerHTML = eval(resultado);
  } else {
    document.getElementById("resultado").innerHTML = "Nada...";
  }
}
 */